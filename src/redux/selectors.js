export const getArticles = (state) => {
   return state.global.articles
}

export const getIsLoading = (state) => {
   return state.global.isLoading
}

export const getTotalResults = (state) => {
   return state.global.totalResults
}